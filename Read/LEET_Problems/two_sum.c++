/* problem: 
given an array of int and a target int 
find what combination of values in the array will get the target value 
or will get the closest to the target value 
*/ 

#include<iostream>
using namespace std; 


int main()
{
    int num_array[] = {2,7,11,15};
    int target = 9;
    int length = sizeof(num_array);

    int index[2]; //two indicies for the values 

    //first gathering the two const 

    //for( )

    cout << length << endl;
    
}