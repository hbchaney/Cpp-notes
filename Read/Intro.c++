//hello world program 
//we will be including different library than normal c 
#include <iostream>
using namespace std;

int main() 
{
    string name; 
    string extra; 
    name = "Harrison";
    cin >> extra; //to input a value into extra
    cout << name << ' ' << "hello there" << endl; //means consol out in this case 
    cout << "here is some extra string : " << extra << endl; 
    return(0);
}